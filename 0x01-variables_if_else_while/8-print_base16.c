#include <stdio.h>

/**
 * main - main block
 * return always returns 0
 */

int main(void)
{
    int b;
    
    for (b = 0; b <= 15; b++)
        printf ("%d", b);
    printf ("\n");
    return (0);
}
